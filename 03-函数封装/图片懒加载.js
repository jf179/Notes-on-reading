(function(win, doc){
    var oImgList = doc.getElementsByClassName('jJimgList')[0],
    data = JSON.parse(doc.getElementById('J_data').innerTHML),
    imgTpl = doc.getElementById(J_imgTpl).innerTHML;
})
// 未写完